# Extra dependencies for running the test suite (tests/). Not needed to run
# the app itself -- install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
httpx==0.28.1
